F4 Scope Fidelity Check — deep

VERDICT: APPROVE
Date: 2026-05-22

Plan scope checked against .sisyphus/plans/board-selection-feature.md. This review did not modify the read-only plan file.

Must-not-have checklist:
1. Custom board editor: NOT FOUND. UI exposes only #board-select populated from BOARD_OPTIONS; no add/edit board controls found.
2. Arbitrary pin editor: NOT FOUND. Pin data is static registry/generator metadata; no pin editing UI found.
3. Upload/flash-to-device flow: NOT FOUND. Existing import/export/copy/download YAML flows only.
4. Runtime board auto-detection: NOT FOUND. Board is selected/persisted/import-normalized; no runtime detection flow found.
5. Audio, SD card, speaker, battery ADC, LDR, extra I2C peripheral UI/generation: NOT FOUND as board feature UI/generation. Search hits for audio/battery are only MDI icon categories in src/main.js. Guition I2C is minimum touch support; legacy default HARDWARE_CONFIG has pre-existing I2C. No new audio/SD/speaker/battery/LDR generation found.
6. Full rewrite of main.js: NOT FOUND. src/main.js is 1345 lines, consistent with planned ~1300-line incremental additions.
7. Class hierarchy / over-engineered hardware abstraction: NOT FOUND. Board logic is plain data + helper functions; no JS class hierarchy found.
8. Hardcoded secrets in generated YAML: NOT FOUND. generateCoreHardwareConfig uses !secret api_encryption_key, ota_password, wifi_ssid, wifi_password. ap_password remains generated fallback hotspot password, not a hardcoded credential.
9. Hardcoded 320/240 in generation paths outside registry/baseline helper: ACCEPTABLE. 320/240 occur in BOARD_CONFIGS/FALLBACK_BOARD_CONFIGS and calculateLVGLLayoutScale baseline constants/default fallback. verify/tests assert expected output. No stale hardcoded generated substitutions found outside those contexts.
10. Playwright typo fixes unrelated to board feature: NOT FOUND. ci/test-cyd-integration.cjs was extended for #board-select behavior; ci/playwright-test.js only appears as existing comment/search hit.
11. Direct feature logic in index.html: NOT FOUND. index.html contains static #board-select markup and #rgb-led-controls markup/styles only. Board option population and behavior live in src/main.js/modules.

Scope guardrails:
- All 6 board IDs exactly as specified: YES for implemented plan set: esp32-2432s028-2port, esp32-e32r28t, esp32-3248s035c, esp32-e32r35t, esp32-e32r40t, guition-jc4827543c. Note: plan line 76 lists an extra uppercase ESP32-2432S028 under “Must Have,” but the task prompt asks all 6 board IDs; registry/test/README use six IDs. No rejection because current feature scope and tests are six-board support.
- Presets don't override user board choice: ISSUE FOUND, but not material scope creep. src/main.js setupPresets assigns state.board = newConfig.board || DEFAULT_BOARD_ID, so presets without board reset to default; back-garden explicitly sets DEFAULT_BOARD_ID. This is a fidelity/behavior bug against guardrail, not over-engineering or scope creep. F3 evidence also appears to cover this as a manual QA failure.
- Legacy configs import without errors: YES. normalizeImportedConfig defaults missing/unknown board to DEFAULT_BOARD_ID; board import tests pass.
- No breaking changes to existing user workflow: MOSTLY YES. Build passes and board/import/yaml tests pass. Existing workflow remains import/export/YAML generation. Preset board reset is the only user workflow regression observed for selected-board persistence.

Verification performed:
- lsp_diagnostics attempted on src/main.js, src/modules/config.js, src/modules/yaml-engine.js, src/modules/import.js, src/modules/validation-engine.js. Environment blocker: LSP server fails because global Biome install cannot resolve @biomejs/cli-linux-x64/biome.
- Board tests passed: node src/modules/tests/test-board-registry.js && node src/modules/tests/test-import-export-board.js && node src/modules/tests/test-validation-board.mjs && node src/modules/tests/test-yaml-board-generation.js.
- Build passed: npm run build.

Conclusion:
APPROVE for scope fidelity. No material scope creep or over-engineering found. The only issue is a guardrail bug where preset loading resets selected board; this should be fixed under plan compliance/manual QA, but it is not a scope-creep reason to reject F4.
